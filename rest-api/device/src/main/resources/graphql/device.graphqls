type Device {
id: Int!,
deviceNumber: String
expiryDate: String
}

type Query {
devices(count: Int):[Device]
device(id: Int):Device
deviceByDeviceNumber(deviceNumber: String):Device
device(fields: Int):Device
}

type Mutation {
createDevice(deviceNumber: String!, expiryDate: String):Device
}